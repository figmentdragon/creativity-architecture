import { NeatConfig } from "./NeatConfig";
export declare class RemoteRepo {
    protected static neat_repos: string;
    protected api_endpoint: string;
    protected raw_endpoint: string;
    protected config: NeatConfig | null;
    protected repository: string;
    protected branch: string;
    protected tree: Array<TreeType>;
    constructor(repository: string);
    static getNeatRepoPath(name: string): Promise<any>;
    getConfig(): Promise<NeatConfig>;
    getTree(): Promise<Array<TreeType>>;
    protected fetchNeatConfig(): Promise<NeatConfig>;
    protected fetchTree(): Promise<Array<TreeType>>;
}
export declare type TreeType = {
    path: string;
    type: "blob" | "tree";
    url: string;
};
